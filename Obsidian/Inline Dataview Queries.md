Embed queries directly into notes for real-time data. Example:
``````dataview
list from #project
where status = "in-progress"
```
```This shows all notes with the `#project` tag and `status` field set to "in-progress."```
